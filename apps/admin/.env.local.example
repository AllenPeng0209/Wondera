# Admin 後端同步：角色列表與詳情從 Backend API 拉取
# 複製為 .env.local 並填入實際值

# 後端 Base URL（必填，admin 的 /api/roles 會代理到此）
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Admin API 認證（僅用於 Next 服務端代理，勿設為 NEXT_PUBLIC_）
# 需與 services/backend/.env 的 ADMIN_USER / ADMIN_PASSWORD 一致
ADMIN_USER=admin
ADMIN_PASSWORD=change_me

# 預覽到 Web 聊天頁的 URL（可選，預設用同機 3001）
# NEXT_PUBLIC_WEB_PREVIEW_URL=http://localhost:3001
